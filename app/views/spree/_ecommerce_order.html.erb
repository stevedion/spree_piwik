_paq.push(['trackEcommerceOrder',
  '<%= order.number %>',
  <%= order.total %>,
  <%= order.item_total %>,
  <%= order.tax_total %>,
  <%= order.ship_total %>,
  false
  ]);
var visitor_id;
_paq.push([ function() { visitor_id = this.getVisitorId(); }]);
console.log("ACCOVIA visitor_id="+visitor_id);
console.log("sd="+_paq);

